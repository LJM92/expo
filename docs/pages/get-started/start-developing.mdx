npx react-native init IMSportsApp
cd IMSportsApp
npm install react-native-webview
react-native link react-native-webview
import React from 'react';
import { SafeAreaView, StyleSheet } from 'react-native';
import { WebView } from 'react-native-webview';

const htmlContent = `
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>IM'Sports</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f4f4f4;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px;
    }

    h1 {
      color: #333;
      margin-bottom: 20px;
    }

    .button {
      width: 100%;
      max-width: 300px;
      padding: 15px;
      margin: 10px 0;
      font-size: 1.2rem;
      color: #fff;
      background-color: #007BFF;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .button:hover {
      background-color: #0056b3;
    }

    .camera-buttons {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 10px;
      margin-top: 20px;
    }

    .camera-button {
      flex: 1 1 45%;
      padding: 10px;
      font-size: 1rem;
      color: #fff;
      background-color: #28a745;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .camera-button:hover {
      background-color: #1e7e34;
    }

    @media (max-width: 600px) {
      .button {
        font-size: 1rem;
        padding: 12px;
      }

      .camera-button {
        font-size: 0.9rem;
        padding: 8px;
      }
    }
  </style>
</head>
<body>
  <h1>IM'Sports</h1>

  <button class="button" onclick="toggleRecording()">Start Recording</button>
  <button class="button" onclick="toggleMainCamera()">Main Camera: On</button>

  <div class="camera-buttons">
    <button class="camera-button" onclick="switchCamera('Camera 1')">Camera 1</button>
    <button class="camera-button" onclick="switchCamera('Camera 2')">Camera 2</button>
    <button class="camera-button" onclick="switchCamera('Camera 3')">Camera 3</button>
    <button class="camera-button" onclick="switchCamera('Camera 4')">Camera 4</button>
  </div>

  <script>
    let isRecording = false;
    let isMainCameraOn = true;

    function toggleRecording() {
      isRecording = !isRecording;
      const recordingButton = document.querySelectorAll('.button')[0];
      recordingButton.textContent = isRecording ? 'Stop Recording' : 'Start Recording';
      // Add logic to start/stop recording
    }

    function toggleMainCamera() {
      isMainCameraOn = !isMainCameraOn;
      const mainCameraButton = document.querySelectorAll('.button')[1];
      mainCameraButton.textContent = isMainCameraOn ? 'Main Camera: On' : 'Main Camera: Off';
      // Add logic to enable/disable main camera
    }

    function switchCamera(cameraName) {
      // Add logic to switch to the selected camera
      alert('Switched to ' + cameraName);
    }
  </script>
</body>
</html>
`;

const App = () => {
  return (
    <SafeAreaView style={styles.container}>
      <WebView
        originWhitelist={['*']}
        source={{ html: htmlContent }}
        style={styles.webview}
      />
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  webview: {
    flex: 1,
  },
});

export default App;
npx react-native run-android
# or
npx react-native run-ios
